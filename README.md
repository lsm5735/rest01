# 개인 이력서 웹사이트

## 구조
```
rest01/
├── index.html          # 메인 페이지
├── css/
│   └── style.css       # 스타일
├── js/
│   ├── data.js         # ✏️ 이력서 내용 (여기만 수정!)
│   └── main.js         # 렌더링 로직
└── images/
    └── profile.jpg     # 프로필 사진
```

## 내용 수정 방법
`js/data.js` 파일의 `RESUME` 객체에서 이름, 경력, 기술 스택 등을 수정하세요.

## 사진 추가
`images/` 폴더에 `profile.jpg` 파일을 넣으세요.
